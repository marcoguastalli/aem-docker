# Add any directories, files, or patterns you don't want to be tracked by version control
.DS_Store
.idea
.idea/*
.idea/misc.xml
*.iml
*.iws
*.lst
out/*
lib/*
target/*
build/*
pom.xml.versionsBackup

crx-quickstart
logging

# AEM SDK binaries (licensed, huge) — keep out of git
src/aem-sdk-*/
*.jar
license.properties

# Dispatcher runtime cache
src/v1/dispatcher/cache/

# Dispatcher config seeded from the Adobe SDK (Adobe-authored, not MIT)
src/v1/dispatcher/src/